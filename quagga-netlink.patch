Fixes CAN-2003-0858; based on RH patch for plain zebra.
--- quagga-0.96.4/zebra/rt_netlink.c.orig	Mon Sep 29 19:06:34 2003
+++ quagga-0.96.4/zebra/rt_netlink.c	Mon Dec  1 13:43:45 2003
@@ -291,6 +291,13 @@
           return -1;
         }
 
+      /* JF: Ignore messages that aren't from the kernel */
+      if ( snl.nl_pid != 0 )
+        {
+          zlog ( NULL, LOG_ERR, "Ignoring message from pid %u", snl.nl_pid );
+        continue;
+      }
+
       for (h = (struct nlmsghdr *) buf; NLMSG_OK (h, status);
            h = NLMSG_NEXT (h, status))
         {
